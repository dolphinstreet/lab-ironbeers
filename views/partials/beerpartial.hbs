     {{!-- <a href="/beers/beer-${id}">    --}}
       <img src="{{this.image_url}}" alt="beer">
    {{!-- </a> --}}
    <div class="random-info">
       <h1>{{this.name}}</h1>
       <h2>{{this.tagline}}</h2>
       <p>{{this.description}}</p>

       <ul class="show-for-random">
        {{#each this.food_pairing}}
            <li>{{this}}</li>
        {{/each}}
    </ul>

    <p class="show-for-random"><b>Brewers tips:</b>{{this.brewers_tips}}</p>
</div>